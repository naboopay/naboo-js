/**
 * NabooApi V1
 * Here you have the first version of the naboo api to create checkout automatically
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TransactionStatusEnum from './TransactionStatusEnum';

/**
 * The CashOutResponse model module.
 * @module model/CashOutResponse
 * @version 0.1.0
 */
class CashOutResponse {
    /**
     * Constructs a new <code>CashOutResponse</code>.
     * @alias module:model/CashOutResponse
     * @param phoneNumber {String} 
     * @param amount {Number} 
     * @param fullName {String} 
     * @param status {module:model/TransactionStatusEnum} 
     */
    constructor(phoneNumber, amount, fullName, status) { 
        
        CashOutResponse.initialize(this, phoneNumber, amount, fullName, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, phoneNumber, amount, fullName, status) { 
        obj['phone_number'] = phoneNumber;
        obj['amount'] = amount;
        obj['full_name'] = fullName;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>CashOutResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CashOutResponse} obj Optional instance to populate.
     * @return {module:model/CashOutResponse} The populated <code>CashOutResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CashOutResponse();

            if (data.hasOwnProperty('phone_number')) {
                obj['phone_number'] = ApiClient.convertToType(data['phone_number'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('full_name')) {
                obj['full_name'] = ApiClient.convertToType(data['full_name'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = TransactionStatusEnum.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CashOutResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CashOutResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CashOutResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['phone_number'] && !(typeof data['phone_number'] === 'string' || data['phone_number'] instanceof String)) {
            throw new Error("Expected the field `phone_number` to be a primitive type in the JSON string but got " + data['phone_number']);
        }
        // ensure the json data is a string
        if (data['full_name'] && !(typeof data['full_name'] === 'string' || data['full_name'] instanceof String)) {
            throw new Error("Expected the field `full_name` to be a primitive type in the JSON string but got " + data['full_name']);
        }

        return true;
    }


}

CashOutResponse.RequiredProperties = ["phone_number", "amount", "full_name", "status"];

/**
 * @member {String} phone_number
 */
CashOutResponse.prototype['phone_number'] = undefined;

/**
 * @member {Number} amount
 */
CashOutResponse.prototype['amount'] = undefined;

/**
 * @member {String} full_name
 */
CashOutResponse.prototype['full_name'] = undefined;

/**
 * @member {module:model/TransactionStatusEnum} status
 */
CashOutResponse.prototype['status'] = undefined;






export default CashOutResponse;

